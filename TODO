
## Quick

* Create a test-options object

  - Will store timeout
  - Will store ipv4 + ipv6 enabled

* Pass test-options to worker



## Harder

We should use sub-commands so that we have four distinct commands:


1. [x] Parse jobs and add to redis:
    $ overseer enqueue input.txt

2. [x] Pull jobs from redis, execute them in turn:
    $ overseer worker

3. [x] Run jobs from local-file only:
    $ overseer local file.txt

4. [ ] Show queue
    $ overseer stats
    -> $ redis-cli lrange overseer.jobs 0 -1

5. [x] Show version
    $ overseer version

6. [x] Show parsed file
    $ overseer dump ..

## Final

Pimp.

Document.
